{
    IBClasses = (
        {
            ACTIONS = {endPassphrasePanel = id; showGraphWindow = id; showSummaryWindow = id; }; 
            CLASS = CXDocument; 
            LANGUAGE = ObjC; 
            OUTLETS = {passphrasePanel = NSPanel; passphraseTextField = NSSecureTextField; }; 
            SUPERCLASS = NSDocument; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {CLASS = "java.lang.Object"; LANGUAGE = Java; }
    ); 
    IBVersion = 1; 
}